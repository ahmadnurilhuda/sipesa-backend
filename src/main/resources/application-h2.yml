spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/sipesa
    username: sipesa
    password: sipesa
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  h2:
    console:
      enabled: true
      path: /h2-console
  flyway:
    enabled: true
app:
  cors:
    allowed-origins: http://localhost:3000
